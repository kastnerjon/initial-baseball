# leagues

Feature module for `leagues`.

Rules:

- Keep screens thin.
- Move reusable UI into `components/`.
- Do not put baseball scoring rules here.
- Call backend through feature services/hooks only.
